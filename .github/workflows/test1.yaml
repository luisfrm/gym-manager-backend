name: test1

on: [push]

jobs:
  hola:
    runs-on: ubuntu-latest

    steps:
    - name: Hola Mundo
      run: echo "Hola mundo"

    - name: touch file
      run: touch hello.txt

    - name: Set text
      run: echo "Hello World" >> hello.txt

    - name: Read file
      run: cat hello.txt

  other:
    runs-on: ubuntu-latest
    needs: hola
    steps:
      - name: Other
        run: echo "Hello other"